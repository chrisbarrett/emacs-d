;; -*- lisp-data -*-
;; Packages required by init.el bootstrap (before modules load)
;; These are installed via the module system but configured in init.el
(
 general
 no-littering
 auto-compile
 exec-path-from-shell
 )
